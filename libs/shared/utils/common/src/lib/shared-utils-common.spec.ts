import { sharedUtilsCommon } from './shared-utils-common';

describe('sharedUtilsCommon', () => {
  it('should work', () => {
    expect(sharedUtilsCommon()).toEqual('shared-utils-common');
  });
});
